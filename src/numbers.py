# importing:
from math import *

int_variables = 5
float_variables = 4.5

# we can add, subs, multiply and divide as below:
int_variables + float_variables
int_variables - float_variables
int_variables * float_variables
int_variables / float_variables
int_variables % float_variables

# parse a number to a string
str(int_variables)

# built-in math methods:
abs(-4) # absolute number
pow(4,2) # power
max(4, 9) # return the biggest
min(4, 2) # returns the smallest
round(4.5) # returns the number rounded.
floor(3.8) # round the number down
ceil(2.8) # round the numbe up
sqrt(36) # returns the square root of a given number